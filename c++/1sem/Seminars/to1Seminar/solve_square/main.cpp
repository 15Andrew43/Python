#include <iostream>
using namespace std;

int solve_square(float a, float b, float c, &x1, &x2)
{
    return 0;
}

int main() {
    float a, b, c;
    cin << a << b << c;
    if (a == 0)
    {
        if (b == 0)
        {
            if (c == 0)
            {
                return 1543;
            }
        }
    }

    return 0;
}